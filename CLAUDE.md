# CLAUDE.md — CRM Doscom

Hướng dẫn dự án cho Claude Code. Nội dung dùng chung với Antigravity nằm ở AGENTS.md:

@AGENTS.md

---

Nhắc lại điểm quan trọng nhất: **trước khi viết/sửa code trong `functions/`, đọc `openspec/config.yaml` (mục context) và `openspec/specs/` liên quan.** Thay đổi đáng kể → dùng `/opsx:propose`.
