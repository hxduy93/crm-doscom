node_modules/
.wrangler/
# Secret chạy local (wrangler pages dev tự đọc). KHÔNG BAO GIỜ commit.
.dev.vars
.dev.vars.*
.DS_Store
dist/
__pycache__/

# File trung gian của pipeline lấy dữ liệu: ~10.000 SĐT khách lấy từ Pancake CRM.
# Repo này PUBLIC → KHÔNG commit. Workflow build-lead-to-order.yml fetch rồi dùng
# ngay trong cùng job, chỉ commit lead-to-order.json (số tổng hợp).
data/pancake-crm-contacts.json

# Log của runner cập nhật dữ liệu — chỉ có ý nghĩa trên máy chạy nó, giữ 14 ngày rồi tự dọn.
runner/logs/
